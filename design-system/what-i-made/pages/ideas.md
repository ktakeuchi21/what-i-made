# Ideas page override

- Preserve the master culinary-field-journal direction; do not introduce a separate recipe-app aesthetic.
- Use a two-column photographic grid on phones. Missing and AI-generated images use a quiet local placeholder, never a fabricated food photo.
- Use All, Unmade, and Made as an accessible segmented control. Made is text, not color alone.
- Keep source attribution beside the saved snapshot and make Open original a full-height text target.
- Recipe review is one continuous editable form. Network and validation errors stay inline and receive focus when submission fails.
- Recipe detail uses normal screen navigation. Back restores Ideas query, filter, and scroll position.
